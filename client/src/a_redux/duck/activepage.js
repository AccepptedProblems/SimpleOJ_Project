const initialState = {
    PROBLEMS: "problems", 
    SUBMISSIONS: "submissions",
    RANKING: "ranking"
};

const activePage = (state = initialState, action) => {
    return initialState;
};

export default activePage